# This is a dedicated configuration file intended to be autoloaded on user's logging in.
# Since traditional ones like xlogin, xprofile, profile are not loaded as expected on some environments, this is a workaround instead.
# Assuming to be loaded from a setup located at `~/.config/autostart/`
# Some provisionig scripts may append their indications below
